# Go
/bot
/ptas-bot
*.exe
*.test
*.out

# App data (sqlite db + Railway volume mount)
/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Env / secrets
.env
.env.*
!.env.example

# OS / editor cruft
.DS_Store
*.swp

# Reference material (screenshots of the spreadsheet/receipts/payment
# notifications) — contains real names and transaction data, not for the repo.
/docs/
